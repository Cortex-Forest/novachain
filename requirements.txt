# Nova Chain 核心运行依赖（已在本机 pip freeze 验证的锁版本）
aiohttp==3.14.3
aiosignal==1.4.0
attrs==26.1.0
frozenlist==1.8.0
idna==3.18
multidict==6.7.1
yarl==1.24.5

# TLS 证书生成（cert_gen.py 依赖）
pyOpenSSL==26.4.0

# 抗量子签名（生产必装；未安装时自动回退 Ed25519）
# 注意：PyPI 包名是 liboqs-python（import oqs）；PyPI 上的 `oqs` 是另一个无关包，装错会静默回退 Ed25519。
# 首次 import oqs 会自动下载并编译 liboqs（需 gcc/cmake/git）。
# 本机未安装，无法锁定版本；安装后请按 `pip freeze` 补全：
# liboqs-python==0.16.0
