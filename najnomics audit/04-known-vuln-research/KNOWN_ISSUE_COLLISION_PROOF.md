# KNOWN_ISSUE_COLLISION_PROOF.md — 已知问题碰撞证明（Phase 4C）

> 本文件提供每个 known_or_duplicate 驳回的 root-cause comparison（逐项根因比对）。
> 原则：仅当当前代码路径与已知问题根因完全一致时才驳回；标题/组件相似不足为据。

## KI-001 (H-008)：聊天信箱读取无鉴权

- 假设：P31/D56 — 任意地址可读取他人 inbox。
- 当前代码锚点：nova_node.py:1433-1438 `rpc_chat_inbox` 直接 `messages_for(addr)`，无签名/授权校验。
- 已知问题来源：novachain-threat-model.md TM-008（部分处理：ack 已验签 nova_node.py:1440-1462）。
- 已知根因：信箱读取接口未要求收件人签名。
- 当前假设根因：相同 —— 读取路径无鉴权。
- 完全重叠机制：同一函数、同一缺失校验；P1-6 只修复 ack，未触及 inbox 读取。
- 不独立的原因：无新增代码路径；当前路径 = 已知问题路径。
- 使其独立所需证据：读取路径增加新校验或新入口（当前不存在）→ 若未来出现独立绕过方式再报告。

## KI-002 (H-009)：RPC CORS 全放开

- 假设：P43/D75 — 跨站页面可调用链上 RPC。
- 当前代码锚点：network/rpc.py 中间件设置 `Access-Control-Allow-Origin: *`。
- 已知问题来源：TM-009（开放，未处理）。
- 已知根因：CORS 配置未限制来源。
- 当前假设根因：相同。
- 完全重叠机制：同一配置行，无新入口。
- 不独立的原因：无新根因。
- 使其独立所需证据：攻击者通过 CORS 之外的新通道达成跨站（不存在）。

## KI-003 (H-010)：P2P TLS CERT_NONE

- 假设：P44/P49/D76 — 自签名客户端证书不被校验。
- 当前代码锚点：network/p2p.py:30-37 `ssl.create_default_context(); check_hostname=False; verify_mode=CERT_NONE`。
- 已知问题来源：TM-013（开放）。
- 已知根因：TLS 客户端不校验证书。
- 当前假设根因：相同。
- 完全重叠机制：同一连接建立代码。
- 不独立的原因：无新路径。
- 使其独立所需证据：CERT_NONE 之外的新加密弱点（不存在）。

## KI-004 (H-011)：IP 角色前缀匹配残余

- 假设：P45/D77 — `check_ip_limit(ip, role)` 用 `startswith(role)` 前缀匹配可被伪装角色名绕过。
- 当前代码锚点：network/security.py:37-47。
- 已知问题来源：docs/AUDIT_2026-08-13.md P1-8（部分修复：内存上限已加，前缀匹配残余）。
- 已知根因：角色字符串前缀匹配。
- 当前假设根因：相同。
- 完全重叠机制：同一行代码，修复范围未覆盖。
- 不独立的原因：无新攻击面；残余项已在历史报告中记录。
- 使其独立所需证据：前缀匹配被用于新的高价值接口（当前仅限速用途）。

## 附：TM-003 补块时间戳（H-016 备注）

- consensus.py 补块超时判定使用签名者自报时间戳；与 TM-003 同根因（PoS 补块/slash 滥用部分处理），
  本审计独立追踪确认当前代码未引入新的时间戳利用路径，按已知处理，不重复报告。

## 结论
4 项 known_or_duplicate 全部为「同一根因、同一代码路径」，碰撞证明成立；无标题误杀。
