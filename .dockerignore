# VCS
.git
.gitignore

# Python 环境与缓存
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.venv-test/
venv/
env/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# 测试与临时脚本（运行时不需要）
test_*.py
*_tmp.py
tmp/
pytest.ini
najnomics audit/

# 状态 / 证书 / 日志（容器内重新生成，绝不打进镜像）
*.pem
*.key
*.log
chain_state*.json
state_*.json
*_chat.json
_sdk_err.log

# 文档（仓库里保留，镜像不携带以减小体积）
docs/
